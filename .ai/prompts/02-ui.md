Change the UI to look similar to the file inside folder `.ai/ui/print.png`.

Here is a description of the image like an alt text:

- There is a gold border of around 4px of stroke wrapping the slot machine with an inside padding of around 16px.
- Inside the content of the border, the background color is a gradient that goes from a navy blue to a darker one.
- On top of the reels, there is a title "SLOT MACHINE" written in black with gold background. It is similar to a button.
- Under the title is the reels itself, with a black border of around 4px of stroke.
- Under the reel is the button GIRAR, its color is black and background color is a pale green.
